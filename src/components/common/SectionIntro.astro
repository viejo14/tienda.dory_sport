---
interface Props {
  eyebrow: string;
  title: string;
  copy: string;
}

const { eyebrow, title, copy } = Astro.props;
---

<div class="space-y-5">
  <span class="eyebrow">{eyebrow}</span>
  <h2 class="section-title max-w-4xl">{title}</h2>
  <p class="section-copy">{copy}</p>
</div>
